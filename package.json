{
  "name": "felix-thomas-dev",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "sync-assets": "rm -rf server/showcase && git clone https://$GITHUB_TOKEN@github.com/Signifikant-zz/showcase-assets.git server/showcase && mkdir -p .output/server/server && cp -r server/showcase .output/server/server/"
  },
  "dependencies": {
    "@hypernym/nuxt-gsap": "^2.4.3",
    "@nuxtjs/supabase": "^2.0.6",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "mime": "^4.1.0",
    "nuxt": "^4.4.4",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  }
}
